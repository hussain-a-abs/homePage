rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
    	// write only allowed for me, read is public
      allow write: if request.auth.uid == 'IwICdvjSTKUPkdqSeGXmd9apvUZ2';
      allow read: if true;
    }
  }
}